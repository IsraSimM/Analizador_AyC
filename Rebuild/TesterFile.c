#include <stdio.h>
#include <conio.h>
#include <math.h>
int main()
{
    float sueldo_diario, sueldo_semanal;
    int no_empleado, edad, faltas;
    char categoria;
    for (c = 1; c <= 50; c++)
    {
        printf ("escribe tu categoria ");
        scanf(" %d", &no_empleado);

        if (categoria == 1)
            {
                comision = 0.2;
            }else{ 
                comision = 0.1;
            }
sueldo_semanal = (sueldo_diario*comision -(faltas * 650.45));
suelto_total = sueldo_total + suledo_semanal;
    }
    
    printf("Su sueldo es ");
    printf (sueldo_semanal);
    printf ("El monto total de sueldos es ");
    printf (sueldo_total);
}